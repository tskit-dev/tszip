name: Build Docs
on:
  merge_group:
  pull_request:
  push:
    branches: [main]
    tags:
      - '*'

permissions:
  contents: read

jobs:
  docs:
    uses: tskit-dev/.github/.github/workflows/docs.yml@v19
